# Gino，看這裡就好

這個檔案是寫給你自己看的。每次隔一陣子回來，先看這一頁，就知道上次做到哪、改了什麼、接下來要幹嘛。
技術細節不在這裡，在 `docs/` 裡面。

---

## 這是什麼

你的個人記帳系統。網頁版，手機加到主畫面就跟 App 一樣。
資料存在雲端（Supabase），手機跟電腦看到的是同一份。

**怎麼打開**

- 手機：主畫面那個圖示（Safari →「加入主畫面」加的）
- 電腦：瀏覽器開部署好的網址
- 要在自己電腦上跑：`npm run dev`，然後開 <http://localhost:3000>

登入密碼就是 `.env.local` 裡的 `APP_PASSWORD`，你自己設的那組。

---

## 現在能做什麼

- 記帳：支出、開伙、收入、暫付款四種，選分類、選日期、加備註
- **用講的記帳**：打（或用手機麥克風講）一句話，它幫你把表單填好，你確認才記
- **在 LINE 上記帳**：傳一句話就記，回「改成 200」可以改。每晚 10 點沒記帳會提醒你
- 看明細：分月份翻，可以依性質、分類、備註文字篩選
- 改帳：記錯了直接進去改，改過什麼會留紀錄（估算金額改成實際金額就是走這條）
- 分類自己管：新增、改名、停用（停用不會影響以前記過的帳）
- 待結清：押金、代墊、借出去的錢，沒結清首頁一直提醒你
- **月結算**：這個月花多少、近半年趨勢、錢花在哪個分類，月份可以前後翻

## 還沒做

- 把以前的 Excel 匯進來 — **這個需要你先給我一份實際的月度 Excel 檔**
- 匯出成 CSV／Excel
- 跨月比較（這個月 vs 上個月哪裡花比較多）

---

## LINE 怎麼開（只要做一次）

程式已經寫好了，但要你去申請 LINE 的帳號、把幾個值填進 Vercel 才會動。

**一、申請（約十分鐘）**

1. 開 <https://developers.line.biz/console/>，用你平常的 LINE 帳號登入
2. 建一個 **Provider**（名字隨便）
3. 底下 **Create a new channel** → 選 **Messaging API**
4. Channel name 就是這個帳號在 LINE 裡顯示的名字（例如「記帳」）
5. 建好後複製三樣：
   - **Basic settings** 分頁 → **Channel secret**
   - **Basic settings** 分頁 → **Your user ID**（`U` 開頭那串）
   - **Messaging API** 分頁 → **Channel access token (long-lived)** → 按 Issue
6. **Messaging API** 分頁 → **Auto-reply messages** → 進 LINE Official Account Manager
   把「自動回應訊息」**關掉**（不關的話罐頭回覆會蓋掉我們的回應）
7. 掃 **Messaging API** 分頁上的 QR code，把這個帳號加為好友

**二、填進 Vercel**（Settings → Environment Variables，三個環境都勾）

| 名稱 | 值 |
|---|---|
| `LINE_CHANNEL_SECRET` | 上面第 5 步的 Channel secret |
| `LINE_CHANNEL_ACCESS_TOKEN` | 上面第 5 步的 access token |
| `LINE_USER_ID` | 上面第 5 步的 Your user ID |
| `CRON_SECRET` | 自己隨便打一串 20 個字母數字，只是拿來擋人的 |

**不要**設 `LINE_API_BASE`，那是本機測試用的。

**三、把 webhook 網址填回 LINE**

回到 **Messaging API** 分頁 → **Webhook URL** 填
`https://你的網址/api/line` → 按 **Verify** 應該會成功 → 把 **Use webhook** 打開。

**四、Redeploy**（Deployments → 最新那筆 ⋯ → Redeploy）

環境變數是部署時讀進去的，不重新部署不會生效。

弄完在 LINE 傳「午餐 100」試試看。

---

## 更新日誌（新的在上面）

### 2026-08-21（凌晨）　圖表做好了，介面也整個換了一輪

**多了一個「月結算」分頁**（導覽列中間那個），點進去看得到：

- 這個月花了多少，變動／固定用一條長條分開
- 收入、暫付款、收支淨額三格分開放（暫付款永遠不會被算進支出裡）
- **近半年的柱狀圖** —— 點某個月就跳到那個月
- **花在哪的甜甜圈** + 排行榜，點某個分類會跳到只看那個分類的明細
- 左右箭頭可以往前往後翻月份

首頁那張月結算卡片下面也放了同一張趨勢圖的縮小版，點一下就進月結算頁。

**介面整個調過，比較像 app 了：**

- **導覽列有圖示了**。純文字的分頁列一看就是網頁，加上圖示之後手感差很多。
  待結清有幾筆會用一個小圓點標在圖示右上角。
- 首頁最上面改成打招呼（早安／午安／晚安，看你幾點開）＋ 日期靠右
- 「記一筆」整塊收進一張卡片裡，跟下面的內容分得更開
- 卡片圓角變大、顏色有淡淡的漸層，不再是一格一格的方塊
- 手機上按下去會有一下明暗回饋（手機沒有滑鼠停留效果，沒這個會一直懷疑有沒有按到）
- 長按連結不會再跳出「拷貝／分享」那個選單了

**電腦上按快捷鍵記帳**：`scripts/快速記帳.ahk`，按 Ctrl+Alt+J 跳出小輸入框，
打一句話按確定，瀏覽器就會開記帳頁而且那句話已經填好了。
要先裝 AutoHotkey v2，然後把腳本裡的網址換成你的。
**這支我沒辦法測**（這台電腦沒裝 AutoHotkey），你跑跑看，壞了跟我說。

順帶做出來的東西：網址後面加 `?say=一句話` 就會把口語輸入框先填好。
所以你也可以用 iPhone 的「捷徑」App 做一個一樣的東西，不用等我寫。

**圖表長什麼樣**：`screenshots/` 裡的 `1-首頁`、`2-月結算`、`3-月結算-上個月`。
那些是我塞了半年的假資料去截的，好讓你看清楚圖表的樣子 —— 截完就刪乾淨了，
你的帳本裡還是只有你自己那兩筆。

驗收清單從 57 項加到 **70 項，全過**，而且連跑兩次都過。

### 2026-08-20（晚一點）　可以在 LINE 上記帳了

不用開 App 了。在 LINE 傳一句話給你的記帳官方帳號，它就記進去，然後回你記了什麼。

**怎麼用**

```
你 ▸ 剛剛跟朋友吃午餐花了150
機器人 ▸ 記好了 · 餐食 −150
         8/20 跟朋友吃午餐
         不對的話回「改成 200」或「刪掉」

你 ▸ 改成 180
機器人 ▸ 改好了 · 餐食 −180
         原本是 150
```

能回的話只有三個：**改成 ○○**（改金額）、**刪掉**（刪掉剛剛那筆）、**說明**（看用法）。
其他都會被當成新的一筆帳。

**跟網頁不一樣的地方**：網頁是填好給你確認才記，LINE 是**先記進去**，
不對再回一句改。聊天視窗裡跳確認很煩，而回一句話就能改，等於確認往後移了。

**每天晚上 10 點會提醒你**——但**只有那天完全沒記帳才會發**。
每天固定跳一則很快就會被無視，只在真的漏記那天出現才有意義。
時間會有誤差（可能 10:00～10:59 之間才到），這是免費方案的限制，不是壞了。

**只有你能記帳。** 別人就算加了這個官方帳號、傳訊息進來，也是石沉大海。

**要先設定才會動**，步驟在上面「LINE 怎麼開」那段。

**順手拆掉一顆地雷**

驗收腳本原本有一句清理指令是「刪掉所有 LINE 記的帳」。等你真的開始用 LINE 之後，
跑一次驗收就會把你在 LINE 記的帳全部刪光。已經改成只刪帶測試標記的那些。
（口語輸入跟 LINE 記的帳，備註是 AI 寫的、不會帶標記，所以改成從原句那張表反查。）

驗收清單從 44 項加到 **57 項，全過**。

### 2026-08-20　可以用講的記帳了

記帳頁最上面多了一條輸入框。打一句白話，它幫你把下面的表單填好，**你看過、按下去才會真的記進去**。

**怎麼用**

打「剛剛跟朋友吃午餐花了150」→ 等一秒多 → 下面就變成「支出／餐食／150／今天」，
上面會顯示「聽成這樣：……」讓你對一眼。對就按「記一筆」，不對就直接在下面改，
改完再按。它只是幫你填，最後一關永遠是你。

**用手機的話不用打字**：鍵盤上那個麥克風鈕直接講就好，講完送出，效果一樣。

**它聽得懂這些**

| 你講 | 它填 |
|---|---|
| 剛剛跟朋友吃午餐花了150 | 支出／餐食／150／今天 |
| 今天在家開伙 | 開伙／不記金額 |
| 昨天幫室友墊了房租8000 | 暫付款／8000／昨天 |
| 買菜大概三百多吧 | 支出／食材採買／300／**自動勾了估算金額** |
| 今天天氣真好 | 不填，跟你說「看不太出來是在記什麼」 |

**⚠ 一定要看一眼再按**

AI 會猜錯，而且錯的方式常常是**金額不見**。開發時就遇過一次「買菜大概三百多吧」
被它讀成開伙 0 元 —— 三百塊直接蒸發。現在這個情況修好了，但同類的錯還是會有。
所以設計上刻意不讓它自己寫進去，一律要你按。花一秒掃一眼金額，比事後對帳快得多。

**其他小事**

- 你講過的每一句都會存起來（不管有沒有採用），之後可以拿來看它常常誤判什麼、再去調它。
  這期沒做翻看的頁面，要看的話 `npm run db:studio`，開 `raw_inputs` 那張表。
- AI 掛掉或沒設金鑰時，記帳頁照常能用，只是輸入框會跟你說一聲，你就手動填。
- 解析失敗**不會**清掉你已經手動填到一半的東西。
- 驗收清單從 35 項加到 **44 項，全過**。

### 2026-08-18　把版面重新排過，順便整理檔案

**你反應的兩件事，都處理了：**

1. **畫面太靠上、被手機上面的時間電量擋住**
   從主畫面開啟時是全螢幕，程式沒有幫瀏海那一段留位置。現在每一頁最上面都會
   自動空出安全距離，被擋住的問題不會再有。

2. **一打開資訊太多、不知道要看哪裡**
   整頁重新分層，改成由上而下四段：**今天幾號 → 記一筆 → 這個月花多少 → 最近記了什麼**。
   具體改了這些：

   - **「開伙」升格成一個按鈕**。以前它是藏在下面的一個勾勾，現在跟支出、收入、
     暫付款並排，一眼就看得到。點下去金額欄自動變成「不記金額」，不用再自己想。
   - **備註、估算金額、固定支出收起來了**，變成一條「備註與標記」，要用才點開。
     十次記帳有九次用不到這三個，攤在那裡只是佔位置。有填東西時會標「已填 N 項」，
     不會忘記。（點進去改舊帳時，如果本來就有填，會自動幫你打開。）
   - **月結算卡片只留有數字的東西**。那個月沒收入就不會出現「收入 0」這種空行。
     最上面是一個大數字（這個月花了多少），下面一條長條圖分變動／固定，其餘才是配角。
   - **首頁的待結清提醒壓成一行**。以前會列三筆明細，跟記帳表單搶注意力。
   - **明細頁的搜尋跟分類篩選也收起來了**，變成一個「篩選」按鈕，有在用時會標「使用中」。
   - **明細頁的分類篩選分成「支出分類」「收入分類」兩排**。以前混在一起，
     兩邊各有一個「未分類」，看起來像同一顆按鈕出現兩次。
   - 首頁「最近」從 8 筆減成 4 筆。要翻就去明細頁。

**檔案整理：**

- 規格書、實作計畫搬進 `docs/`，根目錄清爽很多
- `早上看這個.md` 刪掉了，裡面有用的東西整理成 `docs/踩過的雷.md`（那些查過的坑）
- 刪掉 Next.js 附的範例圖檔（那五個 svg 從來沒用到）
- 多了這個檔案

**已經在 iPhone 尺寸下實際跑過一遍**，每一頁的截圖在 `screenshots/`
（`2-首頁`、`5-明細`、`9-改一筆帳` 那幾張）。連資料庫還是偶爾會卡住
（這台電腦的老問題，`docs/踩過的雷.md` 有記），但多試一次就過。

**部署完還是麻煩用手機點一下這幾個地方**（安全區那段只有真機看得出來）：

1. 最上面那行日期有沒有被時間、電量擋住
2. 記一筆午餐 150 → 存得起來嗎
3. 按「開伙」→ 金額欄有沒有變成「不記金額」→ 存下去是不是 0 元
4. 點開「備註與標記」→ 打字、勾估算 → 存下去有沒有生效
5. 明細頁點「篩選」→ 選一個分類 → 清單有沒有跟著變

哪一項怪怪的跟我說是第幾項就好。

### 2026-08-18（同一天，晚一點）　黑畫面的真兇抓到了

你講的「記帳這頁第一次好、之後切回去一直轉黑畫面」，查出來了，**不是延遲，是 bug**。

**發生什麼事**：程式跟資料庫之間的連線會變成殭屍 —— 查詢送出去，回應永遠不回來。
不是慢，是真的不回來。而且那幾條連線一起死，死了就不會自己好，只能重開伺服器。

**為什麼只有記帳頁中招**：只有那一頁一次要查四樣東西（分類、當月、最近、待結清），
等於一次要開四條新連線，而「一次開多條新連線」正是最容易出事的時候。
明細、分類那些一次只查一兩樣，所以你感覺不到。

**改了四件事**：

1. **查詢超過 3 秒沒回應，就把整組連線丟掉重建、重試一次。**
   這是最關鍵的一條。以前只能一直轉到天荒地老，現在最多頓一下就自己好。
2. 關掉 postgres.js 開新連線時那支多餘的內建查詢（它會被砍掉，還連累後面的查詢）。
3. 連線數上限從 3 拉到 5，不然查詢會被硬塞到同一條連線上，後面那支會失蹤。
4. 每一頁的資料改成**一支一支查**，不要一次全發。本機慢約 150 毫秒，
   部署到 Vercel 上跟資料庫同機房只差幾毫秒，換來不會卡住很值得。

**怎麼確認的**：那份 35 項的驗收清單，以前在這台電腦上從來沒跑完過，
現在 **35 項全過、0 失敗**。而且我故意把「重建連線」關掉再跑一次，
馬上又只跑得完 3 項就死 —— 所以確定是這條在救。

順便修了驗收腳本本身的三個毛病（寫死當月金額、抓到收合區裡看不見的元素、
點分頁沒等網址換過去），細節在 `docs/踩過的雷.md`。

> 補充一句：這跟「要不要搬到甲骨文」是兩件事。這個 bug 在哪裡跑都會發作，
> 只是你家網路比較容易觸發。搬家那件事的評估還是照下面那段。

### 2026-08-11　修掉四個會出事的問題

資料庫連線每查一次就多開一組、離線頁面被存成登入頁、service worker 中途搶頁面、
登入卡在「驗證中」。都修了，細節在 `docs/踩過的雷.md`。

### 2026-08-10　系統做出來了

P0 + P1 完成：記帳、明細、修改、分類管理、待結清、密碼登入、PWA。

---

## 接下來（照順序）

1. **把 LINE 開起來**，步驟在上面「LINE 怎麼開」那段（十分鐘）
2. **看一下新的介面順不順眼**，哪裡怪跟我說
3. **想匯入以前的 Excel 的話，丟一份給我** —— 這是唯一卡在你這邊的事
4. **搬到自己的伺服器**（下面單獨講；黑畫面修掉之後，這件事的必要性大幅下降）

---

## 關於「開起來很久」這件事

你說手機每次開都要等，想搬到本地端或甲骨文的伺服器。我查了一下，先講結論：

（2026-08-18 補充：你後來發現本機也會卡，那是上面那個連線 bug，已經修掉了。
下面講的是「就算沒有 bug，第一下還是要等一下」的部分。）

**慢的原因不是資料庫在遠端，是 Vercel 免費方案會把伺服器「睡掉」。**
沒人用的時候程式根本沒在跑，你點開的那一下它才臨時開機、重新連資料庫，
這就是那 1～3 秒。之後連續操作就順了 —— 因為它醒著。

所以：

- **搬到家裡的電腦不會比較快**。在家連 Wi-Fi 是快，但人在外面時要從外網繞回家，
  吃你家的上傳頻寬，通常更慢，而且電腦一關就整個不能用。
- **你講的甲骨文方向是對的**。Oracle Cloud 有永久免費的方案，給你一台
  24 小時開著的機器（可以選東京機房），程式跟資料庫裝在同一台，
  沒有「睡掉再叫醒」這件事，資料庫也不用跨網路。這才是真的解法。

**但先別急著搬。** 有三件小事幾乎不花錢就能改善，做完再看還慢不慢：
不要每次都重查所有資料、把畫面外殼先快取起來、定時戳一下讓伺服器不要睡著。
詳細寫在 `docs/實作計畫.md` 第 6 節，真的要搬的時候那邊也有步驟。

搬家要付的代價也先講清楚：備份、更新、憑證到期都變成你自己的事，
Vercel 那邊是別人幫你顧的。

---

## 常用指令

| 想做什麼 | 打這個 |
|---|---|
| 在自己電腦上跑起來 | `npm run dev` |
| 確認程式沒寫壞（部署前跑） | `npm run build` |
| 改完資料表結構要同步 | `npm run db:push` |
| 直接看／改資料庫內容 | `npm run db:studio` |
| 自動點過一遍檢查有沒有壞 | 先 `npm run build && npm run start`，另開視窗 `node scripts/verify.mjs` |

## 檔案放哪

```
Gino.md                  ← 你在看的這個
README.md                ← 第一次設定、部署步驟
docs/規格書.md            ← 你的記帳規則，不能亂改的那些
docs/實作計畫.md          ← 技術決定、分幾期做
docs/踩過的雷.md          ← 查過的坑，別再踩一次
src/                     ← 程式本體
scripts/verify.mjs       ← 自動驗收腳本
scripts/快速記帳.ahk      ← 電腦上按 Ctrl+Alt+J 快速記帳
screenshots/             ← 每一頁長什麼樣
```
