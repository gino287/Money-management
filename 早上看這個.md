# 早安，這是昨晚的進度

一句話總結：**程式全部寫完了，但我沒辦法在你這台電腦上完成驗收 —— 卡在網路，不是卡在程式。**
接下來最該做的事是部署到 Vercel，在那邊驗收（大約 15 分鐘，需要你本人操作）。

---

## 一、卡住的地方（先講這個，因為它決定你早上要做什麼）

你的電腦連 Supabase 的那條連線會**中途卡死**。

我在資料庫端抓到了證據：兩個查詢卡在 `Client/ClientRead` 狀態超過兩分鐘，而且**完全沒有任何鎖定衝突**。翻成白話就是：資料庫已經準備好把資料吐出來了，但傳輸到你電腦的那條線斷在半路，資料庫就一直等，等到兩分鐘上限被系統砍掉。查的還只是一張 15 筆資料的分類表。

現象是這樣：

| 情況 | 結果 |
|---|---|
| 剛開伺服器、連線是新的 | 正常，首頁 130ms |
| 連續操作一陣子之後 | 頁面隨機無限轉圈 |
| 把 node 全部關掉重開 | 又正常了（因為換了新連線） |

我注意到你這台機器走 Tailscale（網路位址是 `100.110.58.106` 這種 CGNAT 區段）。這類 VPN 中轉很容易讓長連線在閒置後靜默斷掉，而 PostgreSQL 的連線正是長連線。**這件事我沒有動你的網路設定去驗證，所以是推測，不是結論。**

**為什麼我判斷部署到 Vercel 後不會有這個問題**：Vercel 的機房到 Supabase 東京是資料中心之間的專線，不經過你家的網路，也不經過 Tailscale。而且那才是你真正要用的環境 —— 你平常是用 iPhone 開網頁，不是在這台電腦跑伺服器。

我已經針對這個問題把連線設定調到最耐操（見下方「昨晚修掉的真實 bug」第 4 條），但治不了根，根在網路那一段。

---

## 二、驗收到哪裡（誠實版）

### 已經確認會動的

- 未登入時所有頁面都被擋下來，而且會記住你原本要去哪一頁，登入後送你回去
- 密碼錯誤不放行
- 密碼正確可以進去
- **六種帳都記得起來，而且規格書 2.2 的規則全部正確**：
  - 開伙記 0 元、但紀錄留著
  - 暫付款是獨立性質，沒有被算進當月支出
  - 房租標記為固定支出、餐食是變動支出，兩者分開
  - 估算金額有標記
  - 收入性質正確
- 月結算數字正確：變動 950（150+800，開伙的 0 不影響）、固定 6,000、收入 12,000、暫付款 500 獨立、開伙 1 次

最後這兩項是整份規格書最核心的部分，它在自動化測試裡完整跑過一次並通過。

### 沒能跑完的

事後修正留稽核、待結清提醒、停用分類、明細篩選、刪除、表單驗證這幾項，**程式都寫好了，我也各自看過運作**，但沒能在同一次連續測試裡跑完 —— 每次跑到一半就撞到上面那個連線卡死。

所以：**我不敢說這幾項「已驗收」，只能說「已完成、待驗收」。**

---

## 三、你早上要做的事（約 15 分鐘）

### 步驟 1：把程式碼推上 GitHub

到 <https://github.com/new> 開一個 **private** repo（名字隨意，例如 `ledger`），**不要勾任何初始化選項**。然後在這個資料夾執行：

```bash
git remote add origin https://github.com/<你的帳號>/<repo名>.git
git push -u origin main
```

### 步驟 2：部署到 Vercel

1. <https://vercel.com> → 用 GitHub 登入 → **Add New → Project** → 選剛剛那個 repo
2. 展開 **Environment Variables**，填三個（值就在你的 `.env.local` 裡，直接複製）：
   - `DATABASE_URL`
   - `APP_PASSWORD`
   - `AUTH_SECRET`
3. **Deploy**，等 2 分鐘

### 步驟 3：驗收

拿到網址後，用 iPhone Safari 開，輸入密碼登入，然後：

1. 記一筆午餐 150 → 看首頁數字有沒有跟著動
2. 記一筆「開伙」（勾開伙，金額不用填）→ 應該顯示 0，且月結算出現「本月開伙 1 次」
3. 記一筆勾「估算金額」→ 再點進去改成別的數字 → 頁面下方應該出現「金額：800 → 1250」
4. 到「待結清」新增一筆 → 回首頁看最上面有沒有常駐提醒
5. Safari 分享 → **加入主畫面** → 從主畫面圖示開啟，確認是全螢幕沒有網址列

**如果這五項在手機上都順，那我昨晚沒跑完的驗收就等於補完了，而且是在真實環境補的。**

如果哪一項卡住或轉圈轉不停，跟我說是哪一項，我接著查。

> 順帶一提：不能用 Chrome 加主畫面，iOS 上只有 Safari 支援。

---

## 四、昨晚修掉的真實 bug

這些都是會影響你實際使用的，不是測試才會遇到的：

1. **資料庫連線每查一次就新開一組連線池**
   我寫的延遲初始化用了 Proxy，但快取只在開發模式生效，正式環境等於每次查詢都新開一池。這個如果上線，Supabase 的連線額度很快就會被吃光，整個站掛掉。

2. **離線頁面會被存成登入頁**
   Service worker 要預先快取 `/offline`，但那個網址被登入檢查擋下來、轉址到登入頁，結果快取起來的是登入頁。你真的在收訊差的地方打開時，看到的會是一張登不進去的登入頁，而不是「目前離線」的提示。

3. **Service worker 會中途搶走頁面控制權**
   造成登入完成後畫面換不過去。已改成等下次開啟才生效。

4. **登入改成最原始的表單送出**
   原本用 React 的 Server Action，成功後要靠瀏覽器端的路由跳轉，實測會卡在「驗證中…」不動。現在改成表單 POST + 伺服器 303 轉址，是瀏覽器原生行為，連 JavaScript 沒載入都能登入。登入是進整個系統的那道門，這裡我選穩定不選漂亮。

5. **連線設定加上保命參數**
   限制併發連線數、閒置 20 秒回收、10 秒連不上就放棄、TCP keepalive、連線最多活 10 分鐘就換新的。這些能減輕第一節那個網路問題的影響，但沒辦法根治。

（另外還有一個 Next.js 16 的 `middleware` 已改名為 `proxy`，順手遷移掉了，免得之後累積技術債。）

---

## 五、給你的檢查工具

確認伺服器在跑之後（`npm run build && npm run start`），另開一個視窗：

```bash
node scripts/verify.mjs
```

這會用真的瀏覽器把整份驗收清單走一遍，逐項打勾或打叉，最後把測試資料清乾淨（測試資料都帶 `[E2E]` 標記，不會弄髒你真正的帳）。

`npm run test:e2e` 是完整版的 Playwright 測試，但它會反覆重載整個站，在你目前的網路下必卡。**等部署到 Vercel 之後這兩個都會比較有意義。**

---

## 六、下一步

P1 驗收完（也就是上面步驟 3 跑順）之後，照原訂計畫是：

**P2 口語輸入** —— 用 DeepSeek 解析「剛剛跟朋友吃午餐花了 150」，顯示「幫你記了：餐食 -150，對嗎？」讓你確認後才寫入。你已經有 API Key 了，隨時可以開始。

再來 P3 才是 LINE Bot 和每日提醒 —— 那個才是真正解決「不用想起來要開 App」的關鍵，也是你說最想要的。
